---
description: 
globs: 
alwaysApply: true
---
# GPT Text Rewriting Chrome Extension Development Rules

## Storage Management Guidelines

### NEW STORAGE STRATEGY (2025-01-02) - IMPORTANT
**新功能儲存策略:**
- **所有新功能的資料儲存都必須使用 Local Storage**
- **已開發成功的功能保持現狀，不需修改**
- **目的**: 避免 Sync Storage 8KB/100KB 限制問題

**Implementation Rules for New Features:**
1. 所有新的輸入框、文本區域 → Local Storage
2. 所有新的設定組合、規則集 → Local Storage  
3. 只有 API 金鑰、模型選擇等小型核心設定 → Sync Storage

**Code Pattern for New Features:**
```javascript
// settings.js - 新功能都用 Local Storage
newFeatureData: '',  // 屬性初始化
'newFeatureData',   // 加入 isLocalStorageKey 方法
this.newFeatureData = localResult.newFeatureData || '';  // 從 local 載入
newFeatureData: this.newFeatureData,  // 儲存到 local
```

### Settings Storage and Loading Issues - UPDATED
**Root Causes:**
1. **載入邏輯缺失**: settings.js 中 loadSettings() 方法缺少某些設定項的載入邏輯
2. **儲存邏輯缺失**: saveSettings() 方法中缺少某些設定項
3. **isLocalStorageKey 方法缺陷**: 沒有正確識別所有需要 local storage 的鍵值
4. **儲存容量限制**: 大型資料超過 sync storage 限制

**Complete Prevention Checklist for New Input Fields:**

**A. 屬性初始化 (settings.js)**
```javascript
propertyName: '',
```

**B. 載入邏輯 (settings.js loadSettings方法)**
```javascript
this.propertyName = localResult.propertyName || '';  // 新功能用 local
```

**C. 儲存邏輯 (settings.js saveSettings方法)**
```javascript
propertyName: this.propertyName,  // 加入 localSettings 區塊
```

**D. Local Storage 鍵值識別 (settings.js isLocalStorageKey方法)**
```javascript
'propertyName',  // 新功能都加入此陣列
```

**E. UI 載入 (popup.js)**
```javascript
inputElement.value = settings.propertyName || '';
```

**F. 事件處理 (popup.js eventHandlerConfig)**
```javascript
'propertyName': { type: 'input', element: inputElement },
```

**Storage Type Decision Rules:**
- **Sync Storage (8KB 限制)**: 
  - API 金鑰、模型選擇、簡單開關狀態
  - **注意**: 僅限已存在的核心設定，新功能避免使用
- **Local Storage (10MB 限制)**: 
  - **所有新功能的資料 (預設選擇)**
  - 大型文本內容、指令、背景知識、替換規則、股票清單

**Testing Checklist:**
1. 輸入內容後關閉彈出視窗，重新開啟檢查是否保存
2. 確認首次安裝時預設值正常載入
3. 確認非首次使用時不會覆蓋用戶設定
4. 確認設定的匯入匯出功能正常
5. **新功能測試**: 確認新功能資料正確儲存到 Local Storage

## Settings Management Architecture

### File Responsibilities
- **Core settings logic**: Unified management in settings.js
- **File operations**: Handled in settings-manager.js  
- **UI interaction logic**: Separated from core logic

### Design Patterns
- **Singleton pattern**: For global settings management
- **Strategy pattern**: For handling different setting types

## Error Handling and Data Validation

### Core Principles
- Check storage operation results
- Handle chrome.runtime.lastError
- Validate setting data integrity before save/load

## Git Best Practices

### Commit Message Standards
- Use concise titles (within 20 characters) describing main changes
- Use [Type] prefix to indicate change type

### Push Strategy
- Execute git pull before pushing to sync remote updates
- Break large changes into multiple small commits

## UI Component Development Guidelines

### Drag and Drop Sorting (2025-06-02)
**常見問題與解決方案:**

**問題1: 拖移時閃爍**
- **原因**: 邊界判斷過於敏感，滑鼠微小移動導致條件頻繁切換
- **解決**: 添加明確的移動標記 `shouldMove`，只有滿足條件才觸發移動

**問題2: 無法拖移到最後位置**
- **原因**: `nextSibling !== null` 檢查阻止了移動到末尾（`insertBefore(element, null)` 是合法的）
- **解決**: 移除不必要的 null 檢查

**問題3: 不該移動時也觸發移動**
- **原因**: 邏輯中 `nextSibling` 預設為 `null`，即使不滿足移動條件也可能與當前位置不同
- **解決**: 添加 `shouldMove` 標記確保只有明確滿足條件才移動

**最佳實踐:**
```javascript
let nextSibling = null;
let shouldMove = false; // 明確的移動標記

// 只有滿足條件才設 shouldMove = true
if (condition) {
    nextSibling = targetElement;
    shouldMove = true;
}

// 檢查移動時加入 shouldMove 條件
if (placeholder && container.contains(placeholder) && shouldMove) {
    // 執行移動邏輯
}
```

## Code Refactoring Experience (2025-06-03)

### Highlights System Refactoring
**虛擬滾動重構關鍵經驗:**

**問題1: 高亮清理不完整**
- **症狀**: 清空輸入框後高亮元素殘留在DOM中
- **根本原因**: 虛擬滾動只更新有內容的組，不主動清理空組
- **解決**: 追蹤活躍組，在更新前清理非活躍組

**最佳實踐:**
```javascript
// 虛擬滾動更新前必須清理非活躍組
const activeGroups = new Set();
// ... 收集活躍組 ...
this.groupHighlights.forEach((groupHighlightMap, groupIndex) => {
  if (!activeGroups.has(groupIndex)) {
    this.clearGroupHighlights(groupIndex);
  }
});
```

## Sync Toggle Status Issue (2025-06-08 20:31)

### 擴充頁重載後同步鈕關掉的問題修正
**問題根源**: 同步狀態儲存不一致導致頁面重載後狀態丟失

**關鍵原因:**
1. `GlobalSettings.isLocalStorageKey()` 沒有識別 `syncEnabled` 為 local storage 鍵值
2. `background.js` 初始化時錯誤地以 sync storage 優先，覆蓋 local storage 的正確值
3. SettingsIO 與 GlobalSettings 使用不同的鍵值（`syncEnabled` vs `autoSyncEnabled`）

**修正內容:**
1. **settings.js**: 在 `isLocalStorageKey` 方法中加入 `'syncEnabled'`
2. **background.js**: 修正初始化邏輯，以 local storage 為準
3. **background.js**: 統一備用實現的行為，只使用 local storage

**驗證結果:**
- ✅ 同步鈕狀態正確存儲在 local storage
- ✅ 頁面重載後狀態保持
- ✅ 同步鈕狀態不會被匯出（符合預期）

**重要規則:**
- 所有同步系統相關狀態必須使用 local storage
- `syncEnabled` 是雲端同步開關，不應同步到雲端
- `autoSyncEnabled` 是其他功能的設定，存儲在 sync storage

## 同步狀態匯出策略調整 (2025-06-08 20:31)

### 用戶需求：同步鈕狀態應該可以匯出
**問題**: 原本設計中 `syncEnabled` 被排除在匯出之外，但用戶希望匯入設定後能自動恢復同步狀態

**解決方案**: 區分「雲端同步排除」和「匯出排除」
- **雲端同步**: `syncEnabled` 不應被同步到雲端（避免循環依賴）
- **匯出/匯入**: `syncEnabled` 應該可以被匯出（用戶偏好設定）

**修正內容:**
- **settings.js**: 在 `getAllSettings` 的 `internalStateKeys` 中移除 `'syncEnabled'`
- **SettingsIO**: 在 `shouldExcludeFromSync(key, 'cloud')` 中仍然排除 `syncEnabled`

**驗證結果:**
- ✅ 同步鈕狀態可以被匯出和匯入
- ✅ 雲端同步仍然正確排除 `syncEnabled`
- ✅ 匯入設定後用戶不需要手動重新開啟同步

## API 接口契約與錯誤處理 (2025-06-08)

### 跨模組方法調用的接口不匹配問題
**關鍵經驗: SettingsIO 與 BackgroundSync 接口不匹配**

**問題描述:**
- background.js 期望 `settingsIO.toggleAutoSync()` 返回 `{ success: boolean }` 格式
- 實際 SettingsIO.toggleAutoSync() 返回的是布林值 `enabled`
- 導致 background.js 中 `if (result.success)` 判斷失敗

**根本原因:**
- 不同模組間沒有統一的 API 契約定義
- 缺乏接口文檔或 TypeScript 類型定義
- 假設了錯誤的返回值格式

**解決方案:**
```javascript
// ❌ 錯誤：假設返回 success 對象
const result = await this.settingsIO.toggleAutoSync(enabled);
if (result.success) { ... }

// ✅ 正確：根據實際返回值處理
const resultEnabled = await this.settingsIO.toggleAutoSync(enabled);
if (resultEnabled === enabled) { ... }
```

**預防措施:**
1. **明確記錄各方法的返回值格式**
2. **在調用前檢查實際實現**
3. **添加輸入/輸出日誌來驗證接口行為**
4. **考慮使用 TypeScript 或詳細的 JSDoc 註釋**

## UI一致性檢查導致重複規則問題 (2025-01-08)

### 問題描述: 同步後取到2倍規則數量
**症狀:**
- 日誌顯示重複調用 "從存儲刷新替換組UI"
- UI一致性檢查總是檢測到UI中有0個規則
- 但存儲中有實際規則（如12個），導致重複刷新

**根本原因:**
1. **錯誤的CSS選擇器**: `_extractUIRules()` 方法使用了錯誤的選擇器
   ```javascript
   // ❌ 錯誤選擇器
   const replaceGroups = document.querySelectorAll('.replace-group.manual-replace');
   const fromInput = group.querySelector('input[placeholder*="尋找"]');
   ```

2. **重複消息觸發**: 同步完成後同時發送 `settingsUpdated` 和 `checkUIConsistency` 消息
3. **時序問題**: 第一次刷新還沒完成，就觸發了UI一致性檢查

**修復方案:**
1. **修正CSS選擇器**:
   ```javascript
   // ✅ 正確選擇器
   const replaceGroups = document.querySelectorAll('.manual-replace-container .replace-extra-group');
   const fromInput = group.querySelector('.replace-input');
   const toInput = group.querySelector('.replace-input:last-of-type');
   ```

2. **添加防重複調用機制**:
   ```javascript
   // 在 _checkManualReplaceConsistency 中
   if (this._consistencyCheckInProgress) {
     console.log('[ReplaceManager] ⏸️ 一致性檢查已在進行中，跳過此次調用');
     return;
   }
   this._consistencyCheckInProgress = true;
   
   // 在 refreshFromStorage 中
   if (this._refreshInProgress) {
     console.log('[ManualReplaceManager] ⏸️ UI刷新已在進行中，跳過此次調用');
     return;
   }
   this._refreshInProgress = true;
   ```

3. **延遲UI檢查**: 確保UI已完全更新後再進行一致性檢查
   ```javascript
   await new Promise(resolve => setTimeout(resolve, 150));
   ```

**重要教訓:**
- **CSS選擇器必須與實際DOM結構匹配**
- **防重複調用機制對於非同步操作至關重要**
- **UI一致性檢查的時機很重要，需要等待DOM更新完成**
- **日誌分析是診斷時序問題的關鍵工具**

**驗證方法:**
觀察日誌中是否還會出現重複的 "從存儲刷新替換組UI" 調用，應該看到防重複調用的跳過訊息。


